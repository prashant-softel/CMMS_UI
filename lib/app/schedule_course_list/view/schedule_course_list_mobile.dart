import 'package:flutter/material.dart';

class ScheduleCourseListMobile extends StatelessWidget {
  const ScheduleCourseListMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
