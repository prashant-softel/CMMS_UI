import 'package:flutter/material.dart';

class WebDrawer {
  WebDrawer({this.mTitle, this.mIcon, this.isActive, this.mRoute});
  
  String? mTitle;
  IconData? mIcon;
  bool? isActive;
  String? mRoute;
}
